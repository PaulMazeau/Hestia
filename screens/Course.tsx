import { ViewBase, Text, View } from "react-native";
import React from "react";

const ListeCourseScreen = () => {
    return (
        <View>
            <Text>Liste de Course screen</Text>
        </View>
    );
};

export default ListeCourseScreen;