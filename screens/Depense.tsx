import { ViewBase, Text, View } from "react-native";
import React from "react";

const GestionDepenseScreen = () => {
    return (
        <View>
            <Text>Gestion dépense screen</Text>
        </View>
    );
};

export default GestionDepenseScreen;